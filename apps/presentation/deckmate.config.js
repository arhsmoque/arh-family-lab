// Deckmate base config — safe to commit.
window.DECKMATE_CONFIG = {
  app: {
    name: "Deckmate",
    storageKey: "deckmate-decks-v1",
    allowAnonymous: true,
  },
  limits: {
    maxDecks: 100,
    maxSlidesPerDeck: 100,
  },
};
